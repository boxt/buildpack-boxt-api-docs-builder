#!/bin/sh

# bin/compile BUILD_DIR CACHE_DIR ENV_DIR
BUILD_DIR=$1
CACHE_DIR=$2
ENV_DIR=$3

export RAILS_ENV=qa
export RACK_ENV=qa

cd $BUILD_DIR
touch "public/gavin-test.txt"
exit 0
#
# bin/rails docs:api:build -- --api-class-name=V2::API --build-dir="$BUILD_DIR/pubic/api-docs"
